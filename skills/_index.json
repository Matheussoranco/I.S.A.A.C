{
  "version": "0.2.0",
  "skills": {
    "generate_localized_greeting": {
      "name": "generate_localized_greeting",
      "input_schema": {
        "type": "object",
        "properties": {
          "style": {"type": "string", "enum": ["boxed", "plain"]},
          "user_name": {"type": "string"},
          "include_emoji": {"type": "boolean"}
        }
      },
      "output_schema": {"type": "string"},
      "task_context": "Generate a friendly greeting response in Portuguese to acknowledge the user and invite them to state their request",
      "success_count": 2,
      "skill_type": "code",
      "tags": ["greeting", "pt-BR", "verified"],
      "file": "generate_localized_greeting.py",
      "evidence": "import"
    },
    "python_sorting_demonstration": {
      "name": "python_sorting_demonstration",
      "input_schema": {},
      "output_schema": {},
      "task_context": "Write a Python script that demonstrates list sorting using the built-in sort() method and sorted() function, with example usage",
      "success_count": 2,
      "skill_type": "code",
      "tags": ["python", "example", "verified"],
      "file": "python_sorting_demonstration.py",
      "evidence": "import"
    }
  }
}
