# I.S.A.A.C. requirements — aligned with pyproject.toml.
#
# CORE (== pyproject `dependencies`): install always.
# EXTRAS (== pyproject `[project.optional-dependencies]`): install via
#   pip install -e ".[browser,desktop,calendar,audio,voice,...]"
# and are listed below COMMENTED for reference — do not uncomment here,
# use the extra instead so the two files cannot diverge again.

# ── Core (pyproject dependencies) ─────────────────────────────
langgraph>=0.2,<1.0
langchain-core>=0.3,<1.0
langchain-openai>=0.2,<1.0
openai>=2.20,<3
langchain-anthropic>=0.2,<1.0
langchain-ollama>=0.2,<1.0
docker>=7.0,<8.0
pydantic>=2.0,<3.0
pydantic-settings>=2.0,<3.0
numpy>=1.26,<2.3
chromadb>=1.0,<2.0
networkx>=3.2,<4.0
typer>=0.12,<1.0
duckduckgo-search>=6.0,<9.0
python-telegram-bot>=21.0,<23.0
apscheduler>=3.10,<4.0
httpx>=0.27,<1.0
beautifulsoup4>=4.12,<5.0
croniter>=2.0,<4.0
rich>=13.0,<14.0
prompt-toolkit>=3.0,<4.0
pillow>=10.0,<12.0
fastapi>=0.115,<1.0
uvicorn>=0.30,<1.0

# ── Extras (see pyproject [project.optional-dependencies]) ────
# browser: playwright>=1.44
# desktop: pywebview>=6.2,<7; pyautogui>=0.9.54; keyring>=25
# calendar: caldav>=1.3; icalendar>=5.0
# audio/voice/multimodal/vision/reasoning/benchmarks: see pyproject
#   faster-whisper>=1.0; sounddevice>=0.4; soundfile>=0.12; webrtcvad>=2.0
#   piper-tts>=1.2; pyttsx3>=2.90; pymupdf>=1.24; python-docx>=1.1
#   python-pptx>=0.6; pytesseract>=0.3; mss>=9.0; z3-solver>=4.13
#   huggingface_hub>=0.24; pyarrow>=15.0

# Dev (pyproject `dev` extra)
pytest>=8.0,<9.0
pytest-asyncio>=0.24,<1.0
ruff==0.15.2
mypy==1.19.1
