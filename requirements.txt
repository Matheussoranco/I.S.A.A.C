# Core — Cognitive Engine
langgraph>=0.2
langchain-core>=0.3
langchain-openai>=0.2
langchain-anthropic>=0.2

# Ollama-first LLM routing
langchain-ollama>=0.2

# Sandbox
docker>=7.0

# Configuration & Validation
pydantic>=2.0
pydantic-settings>=2.0

# Numerical (ARC-AGI grids + audio)
numpy>=1.26

# Knowledge Graphs & Graph-of-Thought
networkx>=3.2

# Skill Library — Embedding Retrieval
chromadb>=0.5

# CLI
typer>=0.12

# Web search (no API key required)
ddgs>=0.1

# Telegram gateway
python-telegram-bot>=21.0

# Scheduled background jobs
apscheduler>=3.10

# Browser automation (optional — used inside UI sandbox)
playwright>=1.44

# Web fetch & scrape (connectors)
httpx>=0.27
beautifulsoup4>=4.12

# Calendar connector
icalendar>=5.0

# Background cron scheduling
croniter>=2.0

# Rich terminal UI
rich>=13.0
prompt-toolkit>=3.0

# CalDAV calendar integration (optional)
caldav>=1.3

# ── Multimodal — Vision ───────────────────────────────────────
pillow>=10.0
mss>=9.0  # cross-platform screen capture

# ── Multimodal — Voice (optional, install voice extra) ────────
# faster-whisper>=1.0
# sounddevice>=0.4
# soundfile>=0.12
# webrtcvad>=2.0
# piper-tts>=1.2
# pyttsx3>=2.90

# Dev
pytest>=8.0
pytest-asyncio>=0.24
ruff>=0.8
mypy>=1.13
